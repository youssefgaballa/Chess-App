import type { ChessBoardState } from "./chessSlice";


// export const saveBoard = (board: ChessBoardState, roomID: number | null) => {
//   // localStorage.setItem("chessBoard", JSON.stringify(board));
//   if (roomID) {
//   }

// };

// export const loadBoard = (): ChessBoardState | null => {
//   const board = localStorage.getItem("chessBoard");
//   return board ? JSON.parse(board) : null;
// };

// export const clearBoard = () => {
//   localStorage.removeItem("chessBoard");
// };
